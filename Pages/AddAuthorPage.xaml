<Page x:Class="Library.Pages.AddAuthorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Library.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Добавление автора">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Margin="5">
                <TextBlock>Фамилия</TextBlock>
                <TextBox PreviewTextInput="TextBox_PreviewTextInput" x:Name="LastNameTb"></TextBox>
            </StackPanel>
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Margin="5">
                <TextBlock>Имя</TextBlock>
                <TextBox PreviewTextInput="TextBox_PreviewTextInput" x:Name="FirsNameTb"></TextBox>
            </StackPanel>
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Margin="5">
                <TextBlock>Отчество</TextBlock>
                <TextBox PreviewTextInput="TextBox_PreviewTextInput" x:Name="PatronymicTb"></TextBox>
            </StackPanel>
            <Button x:Name="AddBtn" Click="AddBtn_Click">Добавить</Button>
        </StackPanel>
    </Grid>
</Page>
