<Window x:Class="Library.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="750">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30*"/>
            <RowDefinition Height="170*"/>
            <RowDefinition Height="25*"/>
        </Grid.RowDefinitions>
        <Image Source="/probiblioteku.jpg" Grid.RowSpan="3" Stretch="Fill"/>
        <StackPanel Orientation="Horizontal"  HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="TitleTb" Background="#FFBA977B"  FontSize="25">Районная библиотека</TextBlock>
            <Button x:Name="ExitBtn" Click="ExitBtn_Click" >Выход</Button>
        </StackPanel>

        <Frame NavigationUIVisibility="Hidden" x:Name="MainFrame" Grid.Row="1"/>

        <Button x:Name="BackBtn" Click="BackBtn_Click" Grid.Row="2" HorizontalAlignment="Left">Назад</Button>

        <Button x:Name="ForwardBtn" Click="ForwardBtn_Click" Grid.Row="2" HorizontalAlignment="Right">Вперед</Button>

    </Grid>
</Window>
