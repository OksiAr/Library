<Page x:Class="Library.Pages.AuthorizationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Library.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AuthorizationPage" >

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Margin="2" HorizontalAlignment="Right" Orientation="Horizontal">
                <TextBlock>Логин</TextBlock>
                <TextBox x:Name="LoginTb" Panel.ZIndex="0"></TextBox>
            </StackPanel>
            <StackPanel Margin="2" HorizontalAlignment="Right" Orientation="Horizontal">
                <TextBlock>Пароль</TextBlock>
                <PasswordBox Height="35" x:Name="PasswordPb" Width="200" Padding="10" Panel.ZIndex="1"></PasswordBox>
            </StackPanel>
            <Button x:Name="EntryBtn" Click="EntryBtn_Click">Войти</Button>
        </StackPanel>
    </Grid>
</Page>
